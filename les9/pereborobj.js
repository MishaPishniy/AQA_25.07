const people = [
    { name: 'John', age: 30, city: 'New York' },
    { name: 'Jane', age: 25, city: 'London' },
    { name: 'Tom', age: 35, city: 'Sydney' }
  ];
  
 /* for ( const person of people){

    console.log(`${person.name} is ${person.age} in ${person.city}`)
  }

  const abuts = people.filter(persons => persons.age >=30)
  console.log(abuts) 

  const infos = people.find( personss => personss.name === "John")
  console.log(infos) 

  const names = people.map(person => `${person.name} and ${person.age}`)
  console.log(names)

  people.sort ((a,b) => a.age - b.age)
  console.log(people)

  people.sort((a,b) => a.name.localeCompare(b.name))
  console.log(people)

  people.sort((a,b) => (a.name > b.name) ? 1 : (a.name < b.name) ? -1: 0)
  console.log(people)*/  


